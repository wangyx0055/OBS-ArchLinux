pkgname=PKGNAME
pkgver=1.0
pkgrel=1
pkgdesc="null package for obs"
arch=('i686')
url="http://www.archlinux.org"
license=('see original')

package() {
        mkdir -p ${pkgdir}/etc
	touch ${pkgdir}/etc/${pkgname}.null
}